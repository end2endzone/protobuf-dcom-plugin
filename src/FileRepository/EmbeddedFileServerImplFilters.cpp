/**
 * This file was generated by
 * bin2cpp v1.3.
 * Copyright (C) 2013-2014 end2endzone.com. All rights reserved.
 * Do not modify this file.
 */
#include ".\EmbeddedFileServerImplFilters.h"
#include <stdio.h> //for FILE
#include <string> //for memcpy
namespace bin2cpp
{
  class ServerImplFiltersFile : public virtual bin2cpp::File
  {
  public:
    ServerImplFiltersFile() {}
    ~ServerImplFiltersFile() {}
    virtual size_t getSize() { return 1316; }
    virtual size_t getSegmentSize() { return 200; }
    virtual size_t getNumSegments() { return 7; }
    virtual const char * getFilename() { return "DCOMPLUGIN_COMPONENT_NAMEServerImpl.vcxproj.filters"; }
    virtual const char * getSegment(size_t iIndex, size_t & oLength)
    {
      oLength = 0;
      if (iIndex >= getNumSegments())
        return NULL;
      const char * buffer = NULL;
      size_t index = 0;
      oLength = getSegmentSize();
      buffer = "\xef\xbb\277<?xml version=\"1.0\" encoding=\"utf-8\"?>\x0d\012<Project ToolsVersion=\"4.0\" xmlns=\"http://schemas.microsoft.com/developer/msbuild/2003\">\x0d\012  <ItemGroup>\x0d\012    <Filter Include=\"Source Files\">\x0d\012      <UniqueId"; if (iIndex == index) return buffer; index++;
      buffer = "entifier>{4FC737F1-C7A5-4376-A066-2A32D752A2FF}</UniqueIdentifier>\x0d\012      <Extensions>cpp;c;cc;cxx;def;odl;idl;hpj;bat;asm;asmx</Extensions>\x0d\012    </Filter>\x0d\012    <Filter Include=\"Header Files\">\x0d\012      "; if (iIndex == index) return buffer; index++;
      buffer = "<UniqueIdentifier>{93995380-89BD-4b04-88EB-625FBE52EBFB}</UniqueIdentifier>\x0d\012      <Extensions>h;hpp;hxx;hm;inl;inc;xsd</Extensions>\x0d\012    </Filter>\x0d\012    <Filter Include=\"Resource Files\">\x0d\012      <Uniqu"; if (iIndex == index) return buffer; index++;
      buffer = "eIdentifier>{67DA6AB6-F800-4c08-8B7A-83BB121AAD01}</UniqueIdentifier>\x0d\012      <Extensions>rc;ico;cur;bmp;dlg;rc2;rct;bin;rgs;gif;jpg;jpeg;jpe;resx;tiff;tif;png;wav;mfcribbon-ms</Extensions>\x0d\012    </Filt"; if (iIndex == index) return buffer; index++;
      buffer = "er>\x0d\012    <Filter Include=\"Services\">\x0d\012      <UniqueIdentifier>{86f289e8-bd86-453a-b64e-31462b9e4ae4}</UniqueIdentifier>\x0d\012    </Filter>\x0d\012  </ItemGroup>\x0d\012  <ItemGroup>\x0d\012    <ClInclude Include=\"DCOMPLUGI"; if (iIndex == index) return buffer; index++;
      buffer = "N_COMPONENT_NAMEServerImpl.h\">\x0d\012      <Filter>Header Files</Filter>\x0d\012    </ClInclude>\x0d\012    <DCOMPLUGIN_ALL_SERVICES_FILTERS />\x0d\012  </ItemGroup>\x0d\012  <ItemGroup>\x0d\012    <ClCompile Include=\"DCOMPLUGIN_COMPON"; if (iIndex == index) return buffer; index++;
      oLength = 116;
      buffer = "ENT_NAMEServerImpl.template.cpp\">\x0d\012      <Filter>Source Files</Filter>\x0d\012    </ClCompile>\x0d\012  </ItemGroup>\x0d\012</Project>"; if (iIndex == index) return buffer; index++;
      oLength = 0;
      return NULL;
    }
    virtual const char * getMd5() { return "8ff05a1b881fd3af14cd75edd302555c"; }
    virtual char * newBuffer()
    {
      size_t size = getSize();
      char * buffer = new char[size];
      if (buffer == NULL)
        return NULL;
      size_t numSegments = getNumSegments();
      size_t segmentLength = 0;
      size_t index = 0;
      for(size_t i=0; i<numSegments; i++)
      {
        const char * segmentBuffer = getSegment(i, segmentLength);
        memcpy(&buffer[index], segmentBuffer, segmentLength);
        index += segmentLength;
      }
      return buffer;
    }
    virtual bool save(const char * iFilename)
    {
      FILE * f = fopen(iFilename, "wb");
      if (!f) return false;
      size_t numSegments = getNumSegments();
      size_t segmentLength = 0;
      const char * buffer = NULL;
      for(size_t i=0; i<numSegments; i++)
      {
        buffer = getSegment(i, segmentLength);
        fwrite(buffer, 1, segmentLength, f);
      }
      fclose(f);
      return true;
    }
  };
  File & getServerImplFiltersFile() { static ServerImplFiltersFile _instance; return _instance; }
}; //bin2cpp
