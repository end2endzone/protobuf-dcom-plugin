/**
 * This file was generated by bin2cpp v2.2.0
 * Copyright (C) 2013-2019 end2endzone.com. All rights reserved.
 * bin2cpp is open source software, see http://github.com/end2endzone/bin2cpp
 * Source code for file 'DCOMPLUGIN_COMPONENT_NAMEServerMain.cpp', last modified 1552247130.
 * Do not modify this file.
 */
#include "EmbeddedFileServerMain.h"
#include <stdio.h> //for FILE
#include <string> //for memcpy
namespace bin2cpp
{
  class ServerMainFile : public virtual bin2cpp::File
  {
  public:
    ServerMainFile() { build(); }
    virtual ~ServerMainFile() {}
    virtual size_t getSize() const { return 2616; }
    virtual const char * getFilename() const { return "DCOMPLUGIN_COMPONENT_NAMEServerMain.cpp"; }
    virtual const char * getBuffer() const { return mBuffer.c_str(); }
    void build()
    {
      mBuffer.clear();
      mBuffer.reserve(getSize()); //allocate all required memory at once to prevent reallocations
      mBuffer.append("// DCOMPLUGIN_COMPONENT_NAMEServerMain.cpp : Defines the entry point for the application.\r\n//\r\n\r\n#define WIN32_LEAN_AND_MEAN    // Exclude rarely-used stuff from Windows headers\r\n#include <windows.h>\r", 200);
      mBuffer.append("\n\r\n#ifdef CMAKE_INTDIR\r\n  #ifdef NDEBUG\r\n    #include \"DCOMPLUGIN_COMPONENT_NAMEServer.dir/Release/DCOMPLUGIN_COMPONENT_NAMEServer.h\"  // interface declaration\r\n  #else\r\n    #include \"DCOMPLUGIN_COMPO", 200);
      mBuffer.append("NENT_NAMEServer.dir/Debug/DCOMPLUGIN_COMPONENT_NAMEServer.h\"    // interface declaration\r\n  #endif\r\n#else\r\n  #include \"DCOMPLUGIN_COMPONENT_NAMEServer_h.h\" // interface declaration\r\n#endif\r\n\r\n#include", 200);
      mBuffer.append(" <objbase.h>\r\n#include \"DCOMPLUGIN_COMPONENT_NAMEServer.h\"\r\n#include <stdio.h>\r\n\r\nint APIENTRY WinMain(HINSTANCE hInstance,\r\n                     HINSTANCE hPrevInstance,\r\n                     LPSTR  ", 200);
      mBuffer.append("   lpCmdLine,\r\n                     int       nCmdShow)\r\n{\r\n  // register/unregister server on demand\r\n  //\r\n  char szUpperCommandLine[MAX_PATH];\r\n  strcpy (szUpperCommandLine, lpCmdLine); // copy com", 200);
      mBuffer.append("mand line and work with it.\r\n  _strupr (szUpperCommandLine);\r\n\r\n  //Detect silent flag\r\n  bool silent = false;\r\n  if (strstr(szUpperCommandLine, \"-S\") || strstr(szUpperCommandLine, \"/S\"))\r\n  {\r\n    si", 200);
      mBuffer.append("lent = true;\r\n  }\r\n\r\n  if (strstr (szUpperCommandLine, \"UNREGSERVER\"))\r\n  {\r\n    HRESULT hr = DllUnregisterServer();\r\n    if (SUCCEEDED(hr))\r\n    {\r\n      if (!silent)\r\n        MessageBox(NULL, \"Unreg", 200);
      mBuffer.append("ister completed\", \"Success\", MB_OK | MB_ICONINFORMATION);\r\n      return 0;\r\n    }\r\n    else\r\n    {\r\n      if (!silent)\r\n        MessageBox(NULL, \"Unregister failed\", \"Error\", MB_OK | MB_ICONERROR);\r\n ", 200);
      mBuffer.append("     return 1;\r\n    }\r\n  }\r\n  else if (strstr (szUpperCommandLine, \"REGSERVER\"))\r\n  {\r\n    HRESULT hr = DllRegisterServer();\r\n    if (SUCCEEDED(hr))\r\n    {\r\n      if (!silent)\r\n        MessageBox(NULL", 200);
      mBuffer.append(", \"Register completed\", \"Success\", MB_OK | MB_ICONINFORMATION);\r\n      return 0;\r\n    }\r\n    else\r\n    {\r\n      if (!silent)\r\n        MessageBox(NULL, \"Register failed\", \"Error\", MB_OK | MB_ICONERROR)", 200);
      mBuffer.append(";\r\n      return 1;\r\n    }\r\n  }\r\n\r\n  // initialize the COM library\r\n  ::CoInitialize(NULL);\r\n\r\n  // register ourself as a class object against the internal COM table\r\n  // (this has nothing to do with ", 200);
      mBuffer.append("the registry)\r\n  DWORD nToken = CoEXEInitialize();\r\n\r\n  // -- the message pump ----------------\r\n  //\r\n  // (loop ends if WM_QUIT message is received)\r\n  //\r\n  MSG msg;\r\n  while (GetMessage(&msg, 0, 0", 200);
      mBuffer.append(", 0) > 0) \r\n  {\r\n    TranslateMessage(&msg);\r\n    DispatchMessage(&msg);\r\n  }\r\n\r\n  // unregister from the known table of class objects\r\n  CoEXEUninitialize(nToken);\r\n\r\n  // \r\n  ::CoUninitialize();\r\n\r\n", 200);
      mBuffer.append("  return 0;\r\n}\r\n", 16);
    }
    virtual bool save(const char * iFilename) const
    {
      FILE * f = fopen(iFilename, "wb");
      if (!f) return false;
      size_t fileSize = getSize();
      const char * buffer = getBuffer();
      fwrite(buffer, 1, fileSize, f);
      fclose(f);
      return true;
    }
  private:
    std::string mBuffer;
  };
  const File & getServerMainFile() { static ServerMainFile _instance; return _instance; }
}; //bin2cpp
